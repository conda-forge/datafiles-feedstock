schema_version: 1

context:
  name: datafiles
  version: "2.5"
  python_min: 3.10

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/datafiles-${{ version }}.tar.gz
  sha256: 432498336b57aaa9e3427f3c2cf0c558b980a01d825661da9df61076b3b84f8e

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python ${{ python_min }}.*
    - poetry-core
    - pip
  run:
    - python >=${{ python_min }}.0,<4.0.0
    - json-five >=1.1.2,<2.0.0
    - ruamel.yaml >=0.18.10,<0.19.0
    - tomlkit >=0.10.1,<0.11.0
    - parse >=1.12.0,<2.0.0
    - cached_property >=1.5.0,<2.0.0
    - classproperties >=0.2.0,<0.3.0
    - minilog >=2.3.0,<3.0.0

tests:
  - python:
      imports:
        - datafiles
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  summary: File-based ORM for dataclasses.
  license: MIT
  license_file: LICENSE.md
  homepage: https://pypi.org/project/datafiles

extra:
  recipe-maintainers:
    - jacebrowning
    - diegoferigo
